import { WideEventView } from '@/components/views/WideEventView';

export const metadata = {
  title: 'Wide Events | I FIXED LOGGING',
  description: 'Deep dive into enriched wide-event telemetry',
};

export default function WideEventPage() {
  return <WideEventView />;
}
