import { TailSamplingView } from '@/components/views/TailSamplingView';

export const metadata = {
  title: 'Tail Sampling | I FIXED LOGGING',
  description: 'Manage and observe tail sampling policies in real-time',
};

export default function TailSamplingPage() {
  return <TailSamplingView />;
}
